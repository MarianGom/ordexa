<div class="stack-6">

    <!-- HEADER -->
    <div class="row-between">

        <div>
            <h1 class="h1">Editar Usuario</h1>
            <p class="sub">
                Modifica los datos del usuario
            </p>
        </div>

        <a href="/usuarios" class="btn btn-outline">
            ← Volver
        </a>

    </div>

    <% if (error) { %>

        <div class="card">
            <div class="card__content">
                <p style="color:#dc2626;font-weight:600;">
                    <%= error %>
                </p>
            </div>
        </div>

    <% } %>

    <div class="card card--shadow-sm">

        <div class="card__content">

            <form class="user-form" action="/usuarios/<%= usuario.id_usuario %>" method="POST">

                <div class="filters">

                    <div class="filters__row">

                        <div class="field">
                            <label>CUIL <span class="req">*</span></label>
                            <input
                                class="input"
                                id="cuil"
                                type="text"
                                name="cuil"
                                inputmode="numeric"
                                minlength="11"
                                maxlength="11"
                                pattern="[0-9]{11}"
                                title="Ingresá exactamente 11 números, sin guiones ni espacios"
                                data-usuario-id="<%= usuario.id_usuario %>"
                                aria-describedby="cuil-status"
                                required
                                value="<%= usuario.cuil %>">
                            <span class="field-status" id="cuil-status" aria-live="polite"></span>
                        </div>

                        <div class="field">
                            <label>Correo <span class="req">*</span></label>
                            <input
                                class="input"
                                id="correo"
                                type="email"
                                name="correo"
                                data-usuario-id="<%= usuario.id_usuario %>"
                                aria-describedby="correo-status"
                                required
                                value="<%= usuario.correo %>">
                            <span class="field-status" id="correo-status" aria-live="polite"></span>
                        </div>

                    </div>

                    <div class="filters__row">

                        <div class="field">
                            <label>Nombre <span class="req">*</span></label>
                            <input
                                class="input"
                                type="text"
                                name="nombre"
                                minlength="2"
                                required
                                value="<%= usuario.nombre %>">
                        </div>

                        <div class="field">
                            <label>Apellido <span class="req">*</span></label>
                            <input
                                class="input"
                                type="text"
                                name="apellido"
                                minlength="2"
                                required
                                value="<%= usuario.apellido %>">
                        </div>

                    </div>

                    <div class="filters__row">

                        <div class="field">
                            <label>Teléfono</label>
                            <input
                                class="input"
                                type="text"
                                name="telefono"
                                inputmode="numeric"
                                pattern="[0-9]*"
                                title="Ingresá solamente números"
                                value="<%= usuario.telefono || '' %>">
                        </div>

                        <div class="field">
                            <label>Domicilio</label>
                            <input
                                class="input"
                                type="text"
                                name="domicilio"
                                value="<%= usuario.domicilio || '' %>">
                        </div>

                    </div>

                    <div class="filters__row">

                        <div class="field" style="width:100%">
                            <label>Observaciones</label>

                            <textarea
                                class="input"
                                rows="4"
                                name="observaciones"><%= usuario.observaciones || '' %></textarea>

                        </div>

                    </div>

                    <!-- ROL SOLO ADMIN -->
                    <% if (Number(user?.id_rol ?? user?.rol) === 1) { %>

                        <div class="filters__row">

                            <div class="field">

                                <label>Rol</label>

                                <select class="select" name="id_rol" required>

                                    <% roles.forEach(r => { %>

                                        <option
                                            value="<%= r.id_rol %>"
                                            <%= usuario.id_rol == r.id_rol ? "selected" : "" %>>

                                            <%= r.nombre %>

                                        </option>

                                    <% }) %>

                                </select>

                            </div>

                            <div class="field">

                                <label>Nueva contraseña (opcional)</label>

                                <input
                                    class="input"
                                    type="password"
                                    name="password"
                                    minlength="8"
                                    placeholder="Dejar vacío para no cambiar">

                            </div>

                        </div>

                    <% } else { %>

                        <div class="filters__row">

                            <div class="field">

                                <label>Nueva contraseña (opcional)</label>

                                <input
                                    class="input"
                                    type="password"
                                    name="password"
                                    minlength="8"
                                    placeholder="Dejar vacío para no cambiar">

                            </div>

                        </div>

                        <input type="hidden" name="id_rol" value="<%= usuario.id_rol %>">

                    <% } %>

                </div>

                <div style="display:flex;justify-content:flex-end;gap:12px;margin-top:30px;">

                    <a href="/usuarios" class="btn btn-outline">
                        Cancelar
                    </a>

                    <button type="submit" class="btn btn-primary" id="btnGuardarUsuario">
                        Guardar cambios
                    </button>

                </div>

            </form>

        </div>

    </div>

</div>
